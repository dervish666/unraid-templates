<?xml version="1.0"?>
<Container version="2">
  <Name>PanelsXR</Name>
  <Repository>dervish/panelsxr:latest</Repository>
  <Registry>https://hub.docker.com/r/dervish/panelsxr</Registry>
  <Branch>
    <Tag>latest</Tag>
    <TagDescription>Latest stable release</TagDescription>
  </Branch>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/dervish666/PanelsXR/issues</Support>
  <Project>https://github.com/dervish666/PanelsXR</Project>
  <Overview>Panel is a WebXR comic reader that streams your self-hosted Komga library into a Meta Quest 3 headset. Resume where your iPad left off, turn pages on the thumbstick, and browse a 3D library of your series. Runs in the Quest browser (no store, no sideload).&#xD;
&#xD;
Set KOMGA_URL to your Komga address and KOMGA_API_KEY to a Komga API key (Komga, Account Settings, API Keys). The key is injected server-side and never reaches the browser.&#xD;
&#xD;
IMPORTANT: WebXR needs HTTPS. Access this behind a reverse proxy (SWAG / Nginx Proxy Manager / Cloudflare Tunnel). A plain http address loads the 2D page but the Quest browser will not enter VR.</Overview>
  <Category>MediaApp:Books</Category>
  <WebUI>http://[IP]:[PORT:80]/</WebUI>
  <Icon>https://raw.githubusercontent.com/dervish666/PanelsXR/main/deploy/unraid/panelsxr-icon.png</Icon>
  <TemplateURL>https://raw.githubusercontent.com/dervish666/unraid-templates/main/dervish/panelsxr.xml</TemplateURL>
  <ExtraParams/>
  <PostArgs/>
  <Config Name="WebUI Port" Target="80" Default="8677" Mode="tcp" Description="Port for the Panel web UI" Type="Port" Display="always" Required="true" Mask="false">8677</Config>
  <Config Name="Komga URL" Target="KOMGA_URL" Default="" Description="Your Komga base URL, e.g. http://192.168.1.10:8080" Type="Variable" Display="always" Required="true" Mask="false"></Config>
  <Config Name="Komga API Key" Target="KOMGA_API_KEY" Default="" Description="Use a DEDICATED READ-ONLY Komga user's key (Komga, Account Settings, API Keys). It is injected server-side and never sent to the browser, but it also bypasses Komga's own auth, so a read-only key caps the blast radius to 'someone saw my comics' instead of full account access." Type="Variable" Display="always" Required="true" Mask="true"></Config>
  <Config Name="Basic Auth User" Target="PANEL_USER" Default="panel" Description="Username for the built-in login (only used if a password is set below)." Type="Variable" Display="always" Required="false" Mask="false">panel</Config>
  <Config Name="Password" Target="PANEL_PASSWORD" Default="" Description="Set a password to lock the whole app + Komga proxy behind a login (the Quest browser remembers it, so it's a one-time login). SAFE BY DEFAULT: if you leave this blank, Panel refuses to serve your library (503) until you set one — unless you set the Auth Mode below to 'none'. Set it here before first run." Type="Variable" Display="always" Required="true" Mask="true"></Config>
  <Config Name="Auth Mode (advanced)" Target="PANEL_AUTH" Default="" Description="Leave blank to use the Password above (recommended). Set to 'none' ONLY if Panel is LAN-only or already gated by a reverse proxy / Cloudflare Access / Authelia — 'none' serves your whole library to anyone who can reach the URL." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
</Container>
